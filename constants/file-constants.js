const maxAllowedFileSize = 100 * 1024 * 1024;
const maxAllowedFileSizeInWords = "100MB";
const cleanupTimeLimit = 0 * 60 * 60 * 1000;

module.exports = {
  maxAllowedFileSize,
  maxAllowedFileSizeInWords,
  cleanupTimeLimit,
};
